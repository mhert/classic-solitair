//! Presentation layer between the engine and the renderer.
//!
//! Turns engine state into layout, hit-testing, drag logic, and animation
//! timing, producing sprite display lists a renderer can draw directly.
