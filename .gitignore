/target
/mutants.out*
.superpowers/
nimbalyst-local/
plans/
# Failure diagnostics the golden-image test harness writes next to the
# committed goldens; never commit them.
*.actual.png
# cxx-qt-build generates this qmlls config with an absolute,
# machine-specific buildDir.
.qmlls.ini
